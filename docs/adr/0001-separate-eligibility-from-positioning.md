# ADR 0001：分离可显示性与定位

Status: Accepted

现有同类工具的主要误报来自把“找到一个坐标”当作“用户正在输入”。InputCue 将 Eligibility 与 Anchor 设为两个独立概念：先用焦点、可编辑语义和选区状态确认是否允许显示，之后才寻找位置。鼠标位置和 I 型指针永远不能建立 Eligibility，只能在可编辑已经确认后作为受控坐标降级。代价是部分非标准控件会不显示，但这符合产品“精确优先于覆盖”的定位，并从架构上消除网页拖选和对话框悬停误报。
