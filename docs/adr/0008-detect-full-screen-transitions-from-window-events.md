# 0008：由窗口事件驱动全屏自动暂停

## 状态

已接受

## 决策

启用“全屏自动暂停”后，前台可见窗口完整覆盖其所在显示器边界时进入自动暂停；普通最大化窗口仅覆盖工作区，不视为全屏。桌面、隐藏窗口和最小化窗口不参与判断。

进入与退出判断复用前台、焦点事件，并监听前台顶层窗口的边界变化事件。全屏期间停止提示渲染、动画计时和输入交互，但保留轻量窗口事件监听，以便退出时自动恢复。该功能不新增定时器或长期轮询。

## 影响

手动暂停仍会停止完整诊断流程，优先级高于自动暂停。全屏自动暂停不改变可编辑资格、输入状态、应用停留会话、点击锚点或其他显示规则。
