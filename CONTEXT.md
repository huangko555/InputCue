# InputCue 领域词汇

本文只定义项目内的统一语言，不描述具体实现。

## 输入上下文

### 输入目标（Input Target）

当前获得键盘焦点、可能接收文本的界面位置。

_Avoid_: 鼠标下方控件、鼠标指针指向的控件。

### 可编辑目标（Editable Target）

已经获得足够证据确认可写的输入目标。

_Avoid_: 仅因出现 I 型鼠标指针而推断可编辑。

### 插入光标（Caret）

属于可编辑目标、表示文本插入位置的标记。

_Avoid_: 鼠标指针、选区边缘、绘图软件光标。

### 文本选区（Selection）

起点与终点不同的文本范围。选区可以位于可编辑目标，也可以位于只读内容。

_Avoid_: 把折叠选区或插入光标称为选区。

### 输入状态（Input State）

用户下一次输入可见字符时所处的中文、英文或大写状态。

_Avoid_: 键盘布局名称、输入法产品名称。

## 判断与定位

### 可显示性（Eligibility）

当前是否允许显示状态提示的语义判断。

_Avoid_: 能否获得一个屏幕坐标。

### 锚点（Anchor）

用于放置提示标记的屏幕空间位置或矩形。

_Avoid_: 可显示性的证据。

### 观察（Observation）

在同一前台窗口、焦点和交互代次下收集的一组事实。

_Avoid_: 跨焦点拼接的缓存结果。

### 代次（Generation）

用于标识一次连续输入上下文的单调编号。上下文变化后，旧代次的结果不再有效。

_Avoid_: 时间戳、重试次数。

### 证据等级（Evidence Grade）

当前判断所依据事实的完整程度，取值为“已确认”“已降级”或“未知”。

_Avoid_: 没有可解释含义的置信度百分比。

### 降级（Degradation）

在可显示性已经确认后，改用精度较低的锚点或刷新方式继续工作。

_Avoid_: 用鼠标位置替代可显示性判断。

## 兼容性

### 应用画像（App Profile）

针对一个可复现应用场景、带版本边界和回归用例的兼容策略。

_Avoid_: 按进程名不断堆叠的临时例外。

### 原因码（Reason Code）

对一次显示、隐藏或降级决策的稳定、非敏感解释。

_Avoid_: 自由文本日志、用户输入内容。
